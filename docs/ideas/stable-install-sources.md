---
title: Replace temporary GitHub main.zip install sources
description: Replace temporary GitHub `main.zip` runtime install sources with stable PyPI, release-tag, or pinned-archive sources.
area: packaging
priority: user-prioritized
trigger: Release builds and install scripts need reproducible, versioned sources.
source: Root IDEAS.md prioritized follow-up
---

## Notes

- Pin to release tags or published packages instead of floating `main` archives.
- Update host install paths and helper resolution accordingly.
