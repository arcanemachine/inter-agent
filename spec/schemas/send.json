{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Send",
  "type": "object",
  "required": ["op", "to", "text"],
  "properties": {
    "op": { "const": "send" },
    "to": {
      "type": "string",
      "description": "Target routing name or unambiguous routing-name prefix. Exact names are resolved before prefixes."
    },
    "text": {
      "type": "string",
      "description": "Direct message text. The server accepts UTF-8 encoded text bytes up to INTER_AGENT_DIRECT_MAX."
    },
    "from_name": {
      "type": "string",
      "description": "Optional display name for the delivered message; defaults to the sender routing name."
    }
  },
  "additionalProperties": true
}
